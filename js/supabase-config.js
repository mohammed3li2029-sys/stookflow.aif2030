/* ============================================================================
   StockFlow — Supabase Configuration
   ============================================================================
   Filled in with your real Supabase project.

   Still required before it goes live:
   1. Run supabase-schema.sql once in: Supabase Dashboard -> SQL Editor.
   2. Authentication -> Providers -> make sure Email is enabled.
   3. Authentication -> Users -> add at least one user (email + password).
      StockFlow only talks to Supabase when you log in with an EMAIL
      address. The demo account (admin / 123456) stays local on purpose
      and never touches Supabase.
   ============================================================================ */

window.STOCKFLOW_SUPABASE_CONFIG = {
  url: "https://acbtagbjcrgwaequustj.supabase.co",
  anonKey: "sb_publishable_BM2iVuKQM9drmVibwfNlqw_KwAHJyK4"
};
